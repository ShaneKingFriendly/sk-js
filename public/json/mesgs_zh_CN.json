{
  "$#{field}_is_required": "[$#{field}] 必填。",
  "$#{field}_must_be_an_email": "[$#{field}] 必须是邮箱。",
  "$#{field}_length_must_be_between_$#{min}_$#{max}": "[$#{field}] 长度必须在 [$#{min},$#{max}] 之间。",
  "$#{field}_length_must_be_greater_than_$#{min}": "[$#{field}] 长度必须大于 [$#{min}]。",
  "$#{field}_length_must_be_less_than_$#{max}": "[$#{field}] 长度必须小于 [$#{max}]。",
  "$#{field}_value_must_be_between_$#{min}_$#{max}": "[$#{field}] 的值必须在 [$#{min},$#{max}] 之间。",
  "$#{field}_value_must_be_greater_than_$#{min}": "[$#{field}] 的值必须大于 [$#{min}]。",
  "$#{field}_value_must_be_less_than_$#{max}": "[$#{field}] 的值必须小于 [$#{max}]。"
}
